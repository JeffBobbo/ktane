todo

rename 'State' to something else
rename all `main.cpp` to `<module>.cpp`
rename `Indicators`/`onIndicators` to `update` or something
module versioning to prevent UB
license
migrate
merge into master
simon's logic needs reworking
